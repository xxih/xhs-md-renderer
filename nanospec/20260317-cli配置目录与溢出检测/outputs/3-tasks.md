## 1. 待办

- [x] 1.1 补充 brief，确认“固定配置目录 + 头像素材 + 溢出反馈”的目标、范围与约束。
- [x] 1.2 产出 `outputs/1-spec.md`，明确默认配置目录、配置优先级、底部安全边距与布局报告要求。
- [x] 1.3 产出 `outputs/2-plan.md`，确定由 `cli` 解析配置、由 `core` 负责统一模型与布局分析。
- [x] 1.4 补充本地 research 记录，给出 `adapt` 结论。
- [x] 1.5 扩展 `core` 配置模型与渲染结构，加入底部安全边距和布局分析产物。
- [x] 1.6 扩展 `core` Node 导出链路，输出 `layout-report.json`。
- [x] 1.7 重构 `packages/cli`，支持固定配置目录、`render.json`、头像文件解析和摘要输出。
- [x] 1.8 更新 README，并完成 `npm run build`、`npm run typecheck` 和 CLI 样例验证。
