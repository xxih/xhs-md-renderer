# 对齐记录：markdown图片正确渲染

## 2026-03-17

- [缺失] 用户当前只明确提出“能够正确渲染 md 中的图片”，尚未指定是否需要同时覆盖 HTML `<img>`、引用式图片、尺寸属性、轮播/拼图等高级语法。
  处理：本轮规格先收敛到标准 Markdown 图片语法 `![alt](src)`，并按当前运行环境支持可解析的图片源；HTML `<img>`、引用式图片和尺寸样式控制暂不纳入本轮。
  影响：`brief.md`、`outputs/1-spec.md`、`outputs/2-plan.md` 与 `outputs/3-tasks.md` 全部按这一范围编写。若后续确认要扩到高级图片语法，再追加新的 align。
