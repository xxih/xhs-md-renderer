## 1. 文档与对齐

- [x] 1.1 补充 `brief.md`，明确本轮聚焦 `note-to-red` 默认样式对齐和配置补齐。
- [x] 1.2 新增 `alignment.md`，收敛本轮范围并记录暂不纳入项。
- [x] 1.3 更新 `outputs/1-spec.md` 与 `outputs/2-plan.md`，固化目标、方案与验收口径。

## 2. 核心实现

- [x] 2.1 扩展 `RenderConfig` 与默认主题，补齐字体、字号、标题标签、用户信息、日期与页脚配置。
- [x] 2.2 重构 `packages/core/src/preview.tsx`，让默认页面样式更接近 `note-to-red` 默认主题。
- [x] 2.3 更新 `apps/web` 的配置面板与交互，暴露新增核心配置项。
- [x] 2.4 更新 `packages/cli` 参数解析与帮助文案，映射新增配置项。
- [x] 2.5 按用户提供的 `1800x2400` 参考图调整默认画布尺寸与组件比例。
- [x] 2.6 优化 Web 预览端外壳与动态缩放，让预览舞台与真实画布比例一致。
- [x] 2.7 按用户提供的新参考图，重新标定导出物的头像、昵称、日期等页眉文字比例。

## 3. 验证

- [x] 3.1 跑构建，确认 `core + cli + web` 通过。
- [x] 3.2 跑一条实际 CLI 导出，确认新增配置进入产物。
- [x] 3.3 回写 NanoSpec 任务状态与实现结论。
- [x] 3.4 复验导出尺寸为 `1800x2400`，并确认 Web 预览按新尺寸正确缩放。
- [x] 3.5 复验新一轮导出图的页眉比例与参考图更接近。
