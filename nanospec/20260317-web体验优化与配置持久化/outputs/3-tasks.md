## 1. 文档收敛

- [x] 1.1 补充 `brief.md`，明确本轮聚焦 Web 中文优先体验、导出、持久化与 chrome 配置。
- [x] 1.2 产出 `outputs/1-spec.md`，固化交付目标、非目标、约束与验收标准。
- [x] 1.3 产出 `outputs/2-plan.md` 与 `outputs/3-tasks.md`，明确实现策略与执行顺序。

## 2. 核心渲染与配置模型

- [x] 2.1 扩展统一配置模型，补齐头像、认证标志及各非正文元素的显隐字段，并把 footer 默认值改为关闭。
- [x] 2.2 重构 `packages/core/src/preview.tsx` 页眉/页脚结构，让头像、认证标志、用户名、handle、日期、footer 全部走统一配置。
- [x] 2.3 让所有非正文元素在预览与导出中共享同一套开关逻辑。

## 3. Web 体验改造

- [x] 3.1 把 `apps/web` 控制面板改成中文优先界面，并压缩布局密度，让常用配置尽量在一个屏幕内可见。
- [x] 3.2 修复右侧预览舞台的偏移问题，确保多页预览稳定居中且滚动自然。
- [x] 3.3 为 Web 增加头像配置与上传入口，并将相关配置持久化到本地浏览器存储。
- [x] 3.4 为 Web 增加直接导出图片包的能力，不再只提供 `pages.json` 下载。

## 4. 验证

- [x] 4.1 跑 `core + web + cli` 的类型检查与构建，确认本轮改动没有破坏现有链路。
- [x] 4.2 手动验证 Web 中文界面、左侧配置压缩效果、右侧预览定位与各开关行为。
- [x] 4.3 手动验证 Web 图片包导出与头像配置持久化。
